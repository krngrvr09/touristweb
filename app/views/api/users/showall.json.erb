{
		"userIds":[<%@memberships.each do |member|%><%=member.user_id%><% if not member.eql? @memberships.last%>,<% end %><%end%>],
		"userNames":[<%@usernames.each do |user|%>"<%=user%>"<% if not user.eql? @usernames.last%>,<% end %><%end%>],
		"whereUsers":[<%@whereusers.each do |user|%><%=user%><% if not @whereusers.eql? @whereusers.last%>,<% end %><%end%>]
		


}